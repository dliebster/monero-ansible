---
- name: Enable monerod systemd service
  hosts: all
  become: true
  remote_user: ubuntu
  gather_facts: true

  tasks:
    - name: Enable monerod systemd
      ansible.builtin.systemd:
        name: monerod
        enabled: true
        masked: false
